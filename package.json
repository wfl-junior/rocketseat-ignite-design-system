{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "ignite-ui",
  "version": "1.0.0",
  "author": "Wallace Júnior",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.5.5"
  }
}
